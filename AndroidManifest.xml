<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.rio.ospf"
          android:versionCode="1"
          android:versionName="1.0">
    <uses-sdk android:minSdkVersion="16"/>
    <application android:label="@string/app_name" android:icon="@drawable/ic_launcher" android:theme="@android:style/Theme.Holo.Light">
        <activity android:name=".BanyakRouter"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".NamaRouter" android:label="@string/inputNama"/>
        <activity android:name=".InputDetailRouter" android:label="@string/inputdetail"/>
        <activity android:name=".Cost" android:label="Informasi Cost Router"/>
        <activity android:name=".InterfaceActivity" android:label="Informasi Interface Router"/>
        <activity android:name=".Ospf" android:label="Informasi OSPF"/>
        <activity android:name=".Menu" android:label="Menu Pilihan"/>
    </application>
</manifest>
